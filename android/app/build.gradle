// DEJA ESTO TAL CUAL - NO MODIFIQUES NADA ARRIBA

android {
    defaultConfig {
        applicationId "com.tuapp.nombre"  // Cambia esto
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }
    
    buildTypes {
        release {
            // ⚠️ PARA PRUEBAS, usa debug:
            signingConfig signingConfigs.debug
        }
    }
}

// NO TOQUES NADA MÁS ABAJO